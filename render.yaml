services:
  - type: web
    name: horizon-dispatcher
    env: python
    plan: free
    buildCommand: pip install -r requirements.trackB.txt
    startCommand: uvicorn app.server:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
    envVars:
      - key: APP_ENV
        value: production
